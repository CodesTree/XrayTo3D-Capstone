# Required return bundle - s2_2_frontend_fold0

Return the executed notebook/log, exact configuration, provenance and history, training and frozen-feature QA figures, job/resource usage, output paths, and path/size/SHA-256 for the front-end checkpoint, trainstates, and feature cache. Include the per-level feature and target hashes. The decoder gate remains blocked until independent review records `PASS`.

