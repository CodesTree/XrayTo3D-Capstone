# Required return bundle - s2_1b_crossview_p2_fold0

Return the executed notebook/log, exact configuration, history CSV, provenance JSON, training curve, resource usage, job/output paths, and path/size/SHA-256 for every large checkpoint. Include paired and shuffled cross-view losses and the upstream P1 hashes. A dependent front-end run remains blocked until independent review records `PASS`.

