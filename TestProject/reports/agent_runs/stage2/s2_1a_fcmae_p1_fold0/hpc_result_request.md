# Required return bundle - s2_1a_fcmae_p1_fold0

Return all of the following before P2 is authorized:

- executed notebook and complete scheduler/job log;
- exact run configuration and manifest SHA-256;
- history CSV and provenance JSON;
- training curve and any additional QA figures;
- GPU model, requested resources, wall time, peak GPU memory, peak host memory, and total GPU memory;
- job ID and all output paths;
- path, size, and SHA-256 for every checkpoint left on HPC;
- traceback and last successful checkpoint if the run fails.

File existence or job completion alone is not a `PASS`.

