# HPC result request - s2_1c_encoder_feature_audit_fold0

Please return both the P1 and P2 executed audit notebooks or logs, each run configuration, `feature_audit_summary.json`, `clean_feature_manifest.json`, local probe metrics/history, AP/LAT similarity CSV, all QA PNGs, resource usage, job/output paths, and path/size/SHA-256 records for large cached features and probe artifacts.

Submission or file existence alone is not evidence of success.
