# Required return bundle - s2_3_decoder_preflight

Return the executed notebook/log, all four configurations, summary JSON/CSV, four histories and capacity curves, resource measurements, job/output paths, and path/size/SHA-256 for resume and final checkpoints. Include explicit observed values for every per-case/per-bone Dice gate, both resume hashes, identical input hashes, and GPU headroom.

